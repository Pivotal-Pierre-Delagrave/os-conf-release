#!/bin/bash


<%
if !filename.end_with? ".sh"
  raise "filename must end with '.sh'"
end
%>

FILENAME="<%=filename%>"

cat << EOF >$FILENAME
<% p('lines').each do |line_hash| %>
  <%=line_hash['line']%>
<% end %>
EOF
